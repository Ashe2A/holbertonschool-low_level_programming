#include "main.h"

/**
 * main - Prints underscore putchar
 *
 * Return: 0
 */
int main(void)
{
char[] upc = "_putchar\n";
for (i = 0 ; i < strlen(upc) ; i++)
{
_putchar(upc[i]);
}
putchar('\n');
Return (0);
}
