#ifndef WEEK9_PROJECT2
#define WEEK9_PROJECT2

unsigned int binary_to_uint(const char *);
void print_binary(unsigned long);
int get_bit(unsigned long int, unsigned int);

int _strlen(const char *);

#endif
