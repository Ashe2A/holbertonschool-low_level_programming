#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print base 10 digits
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 30 ; i <= 39 ; i++)
	{
		putchar(i);
	}

	putchar('\n');

	return (0);
}
