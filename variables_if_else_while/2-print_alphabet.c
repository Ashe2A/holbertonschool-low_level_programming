#include <stdio.h>

/**
 * main - Lists all lowercase letters
 * abcd - Each lowercase letters
 *
 * Return: 0
 */
int main(void)
{
char abcd;
for (abcd = 'a' ; abcd <= 'z' ; abcd++) {
putchar(abcd);
}
return (0);
}
