#include "main.h"
#include <stdio.h>

/**
 * _puts - length of character string
 * @str: the string
 *
 * Return: length of string, 0 if empty
 */

int _puts(char *str)
{
	int i;
	int len = 0;

	for (i = 0 ; s[len] != '\0' ; i++)
	{
		len++;
	}
	return (len);
}
