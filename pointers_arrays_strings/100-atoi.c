#include "main.h"
#include "2-strlen.c"

/**
 * _atoi - first integer in a string
 * @s: string
 *
 * Return: the integer
 */

int _atoi(char *s)
{
	int i, start, final;
	char *integer;

	for (i = 0 ; i <= _strlen(s) ; i++)
	{
		while (*(s + i) >= '0' && *(s + i) <= '9')
		{
				
		}
	}

	for (i = 0 ; i < _strlen(integer) ; i++) 
	{
		
	}

	return (final);
}
